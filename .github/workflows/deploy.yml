name: Deploy

on:
  push:
    branches:
      - source

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: yarn
    - run: yarn build
    - uses: JamesIves/github-pages-deploy-action@v4.2.5
      with:
        branch: master
        folder: dist
